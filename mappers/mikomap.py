class MikoMap():

    def __init__(self, dataset):

        self.dataset = dataset

        self.X = dataset.fpath
        self.Y = dataset.fpath
class ByPass():

    def __init__(self, dataset):

        self.dataset = dataset

        self.X = dataset.X
        self.Y = dataset.Y

